import { Meta, Story, Canvas, Controls } from '@storybook/blocks';



# MenuList
A handy dandy UI component for the menu page. Can be used either as a single entry (no children) or, when used in conjunction with child objects, will display each child object as a list item.

*Designed to be used as the base child component of `MenuGroup` components*


<Canvas />
<Controls />